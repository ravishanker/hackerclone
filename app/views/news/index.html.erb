<h1>Listing news</h1>

<table>
  <!-- <tr>
      <th>Title</th>
      <th>Url</th>
      <th>Text</th>
      <th>User</th>
      <th>Votes count</th>
      <th></th>
      <th></th>
      <th></th>
    </tr> -->

<% @news.each do |news| %>
  <tr>
    <td><%= news.id %></td>
    <td><%= link_to image_tag("grayarrow.gif", alt: "Vote"), '#' %></td>
    <td><%= link_to news.title, news.url %></td>
    <!-- <td><%= news.title %></td>
       <td><%= news.url %></td>
       <td><%= news.text %></td>
       <td><%= news.user %></td>
       <td><%= news.votes_count %></td> -->
       <td></td>
       <% if user_signed_in? %>
       <td><%= link_to 'Show', news %>|</td>
       <td><%= link_to 'Edit', edit_news_path(news) %>|</td>
       <td><%= link_to 'Destroy', news, method: :delete, data: { confirm: 'Are you sure?' } %>|</td>
       <% end %>
  </tr>
<% end %>
</table>

<br />
<%= paginate @news %>
<!-- <%= link_to 'Submit News', new_news_path %> -->
